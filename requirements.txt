# ============================================================
# OISignalFlow Requirements
# Compatible with Python 3.12+ (tested on Ubuntu 22.04/24.04)
# ============================================================
# Install: pip install -r requirements.txt

# Build tools — must be first on Python 3.12 (pkg_resources fix)
setuptools>=68.0.0
wheel>=0.41.0

# Core libraries
schedule>=1.2.0           # Job scheduling
requests>=2.31.0          # HTTP requests for Telegram API
pandas>=2.1.0             # Data analysis — 2.1.0+ has Python 3.12 wheels
python-dotenv>=1.0.0      # Load environment variables from .env

# NSE Data
nselib>=0.7.0             # NSE India data fetcher (free NSE API)

# Notifications
plyer>=2.1.0              # Cross-platform desktop notifications

# Excel support
openpyxl>=3.1.2           # Write Excel files
